{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissões padrão para a janela principal do AgroTrial CRM",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
